# One service per line: name<TAB>url
# Every Cloud Run service that backs a public demo. Warming them costs nothing
# — Cloud Run bills request-processing time, so an instance that is alive but
# idle is free; the ping buys warmth for the price of a few-millisecond request.
model-serving	https://model-serving-wr2e7j4cga-uc.a.run.app/health
# Removed 9 Sept 2026: pcb-defect-detector no longer backs anything.
# Computer-Vision-MLOps-Pipeline runs the model in the visitor's browser now
# (ONNX Runtime Web, static on Vercel), so there is nothing to keep warm and a
# failing ping here would be a false alarm rather than a dead demo.
feature-store-api	https://feature-store-api-wr2e7j4cga-uc.a.run.app/health
search-ranking-api	https://search-ranking-api-yfy6zx2pmq-uc.a.run.app/health
multimodal-rag-backend	https://multimodal-rag-backend-yfy6zx2pmq-uc.a.run.app/health
nlp-pipeline-api	https://nlp-pipeline-api-yfy6zx2pmq-uc.a.run.app/health
# Removed 6 Sept 2026: competitor-intelligence-engine no longer backs
# anything. Competitor-Insight-Engine now runs its API as a Vercel
# service beside its own frontend, so there is no Cloud Run service to
# keep warm and a failing ping here would be a false alarm.
llm-eval-backend	https://llm-eval-backend-bno6qvxnbq-ue.a.run.app/health
# Removed 11 Sept 2026: liver-hcc no longer exists. Cumida-ML-Model exports its
# fitted model parameters and runs both models in the visitor's browser (static
# on Vercel), so the Cloud Run service was deleted along with its Artifact
# Registry repo. Nothing to keep warm, and a failing ping here would be a false
# alarm rather than a dead demo.
